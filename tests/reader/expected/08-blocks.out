single | line:
  (((`|` (a))))
block with two stmts:
  (((`|` (a))) (b))
block at column 2 (inside body):
  ((`=` (f) ((`|` (a) (b)))))
nested blocks:
  ((`=` (f) ((`|` (a) (`=` (g) ((`|` (b) (c))))))))
block as RHS:
  ((`=` (x) ((`|` (`;` (a) (b) (c))))))
